{% extends 'base.html.twig' %}

{% block title %} Create user {% endblock %}

{% block body %}
<div class="container-xxl flex-grow-1 container-p-y">
    <div class="d-flex justify-content-between align-items-center">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Dashboard / users / </span> update</h4>
        <a href="{{path('app.users.index')}}" class="btn btn-info"><i class="bx bx-arrow-back me-1"></i></a>
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">
    {{action}}
    </button>    
    {{ form_end(form) }}
</div>

{% endblock %}