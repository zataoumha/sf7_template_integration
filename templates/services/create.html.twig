{% extends 'base.html.twig' %}

{% block title %} Create service {% endblock %}

{% block body %}
<div class="container-xxl flex-grow-1 container-p-y">
    <div class="d-flex justify-content-between align-items-center">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Dashboard / services / </span> create</h4>
        <a href="{{path('app.services.index')}}" class="btn btn-info"><i class="bx bx-arrow-back me-1"></i></a>
    </div>

    {{form(form)}}
</div>

{% endblock %}